"Xd"   

commands =

cd commands

cd ..

cat

grep

echo "H" > electricity.txt

touch index.html

file electricity.txt

file *

move /?

tree /a

osk                     ( on screen keyboard )

tasklist                ( crl + alt + delete ) keys

alias               ( in user alias folder in c drive )

history           ( determine previous commands in cmnder )

alias /?          ( deletes alias )

ipconfig && mspaint

whoami          ( who am I )

systeminfo


ipconfig   |   clip      (  |   = pipeline )



( site for device list for  10 icon button arrangement )  not a command :

ss64    


Git clone + Github link

dir

cd Git_Course\

comment : Git_Course ( working directory )

stages  =  working directory  +  staging area  +  local repository  +  remote repository 

git status

git add 

git commit

git checkout

git merge

git push 

git fetch


git config --global user.email "example.com"

git config --global user.name "Your Name "

git branch

git remote -v  (result )

git push origin master


staging :

git commit -m "I wrote something"








